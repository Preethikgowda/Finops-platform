# Core AWS SDK — all AWS service calls (Bedrock, Cost Explorer, CloudTrail,
# Athena, DynamoDB, Compute Optimizer, CloudWatch, Pricing API) are made
# through boto3.
boto3>=1.28.0
botocore>=1.31.0

# HTTP — Slack webhook delivery
requests==2.32.3
urllib3==2.2.2
certifi==2024.7.4

# Environment / configuration
python-dotenv==1.0.1

# Logging utilities (structured JSON logging)
python-json-logger==2.0.7

# CS-07: GitHub API client for auto-generating Terraform PRs
# Optional — if not installed, PR generation is skipped gracefully.
PyGithub>=2.1.0
